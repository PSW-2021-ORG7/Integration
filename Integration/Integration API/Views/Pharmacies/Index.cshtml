@model IEnumerable<Integration_Class_Library.Models.Pharmacy>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NamePharmacy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ApiKeyPharmacy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Endpoint)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamePharmacy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ApiKeyPharmacy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Endpoint)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPharmacy">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPharmacy">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPharmacy">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
